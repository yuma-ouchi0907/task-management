import { ArrowDownAZ } from "lucide-react";
import { DEFAULT_ICON_PROPS } from "@/app/tasks/type";

const SortDownArrowIcon: React.FC = () => {
  return <ArrowDownAZ {...DEFAULT_ICON_PROPS} />;
};

export default SortDownArrowIcon;
