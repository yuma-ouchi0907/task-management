import { ArrowDownZA } from "lucide-react";
import { DEFAULT_ICON_PROPS } from "@/app/tasks/type";

const SortUpArrowIcon: React.FC = () => {
  return <ArrowDownZA {...DEFAULT_ICON_PROPS} />;
};

export default SortUpArrowIcon;
