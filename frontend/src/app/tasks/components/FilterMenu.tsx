import FilterIcon from "@/app/tasks/components/FilterIcon";
const FilterMenu = () => {
  return (
    <span className="flex items-center">
      <FilterIcon />
      フィルター
    </span>
  );
};

export default FilterMenu;
