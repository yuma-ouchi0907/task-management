import SortIcon from "@/app/tasks/components/SortIcon";

const SortMenu = () => {
  return (
    <span className="flex items-center">
      <SortIcon />
      ソート
    </span>
  );
};

export default SortMenu;
